<h1>マイページ</h1>
<p>Find me in app/views/public/users/index.html.erb</p>

<h3>投稿した曲</h3>
<table class="table">
	<thead>
		<tr>
			<th>曲名</th>
			<th>投稿日時</th>
	    </tr>
    </thead>
    <tbody>
       <% @users.each do |users| %>
		 	<tr>
		 		<td>
		 			<%= link_to user.band.music.name, public_music_path(music.id) %>
		 		</td>
		 		<td>
		 			<%= bands.created_at %>
		 		</td>
		 	</tr>
	  <% end %>
    </tbody>
 </table>
 <%= link_to "編集ページへ" edit_public_user(current_user.id) %>